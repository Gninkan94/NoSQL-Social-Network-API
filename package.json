{
  "name": "nosql-social-network-api",
  "version": "1.0.0",
  "description": "This project was build for a social media startup, so that the API for the social network that uses a NoSQL database  can handle large amounts of unstructured data.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^7.5.0",
    "node.js": "^0.0.1-security",
    "nodemon": "^3.0.1"
  }
}
